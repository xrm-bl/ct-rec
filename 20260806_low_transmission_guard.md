# 透過率不足ガードの導入 — 何がどう変わったのか (2026-08-06)

対象コミット:

| コミット | 内容 |
|---|---|
| `7e98708` | ガード本体 (ソース 13 本 + `src/blacklim.h` 新設 + ドキュメント 3 本) |
| `c552b44` | 全バイナリ再ビルド (GPU + CPU、ビームライン側で動作確認済み) |

---

## 1. 何が問題だったか (要約)

試料が大きい/高密度で透過信号がダークレベルに達すると、投影値の計算

```c
p = log(I0 / (I - dark));
```

が `I-dark == 0` で **+Inf**、ノイズで `I-dark < 0` になると **NaN** を返していた。
黒投影判定 (`CT_REC_BLACK_THRESH`) は「ラインの**平均**信号 < しきい値」だったため、
視野の端に素抜けの余白が残ると平均に `(1-被覆率)×素抜け信号` のハードフロアが生じ、
しきい値をいくら上げても発火できなかった (被覆率 60% ではしきい値 1000 でも不発)。

素通りした Inf/NaN は GPU リング除去 (`sort_filter_g.cu`) の列ソートを壊した。
列を 2 の冪長に揃える詰め物の番兵 (`FLT_MAX` / 行番号 `-1`) は「必ず上に浮く」前提だが、
**+Inf は FLT_MAX より大きく、NaN はすべての比較が false** のため番兵が実データ領域へ
沈み込み、`perm[]` に `(unsigned)-1 = 4294967295` が入る。書き戻し
`result[perm[e]*Nx + c]` が確保領域の約 35 TB 先を指し、

```
CUDA error sort_filter_g.cu:266: an illegal memory access was encountered
ring removal image processing failed
```

で停止していた (266 行目は違反の発覚点=最初の同期点であって発生点ではない)。
CPU 版は停止しない代わりに、qsort の比較関数が NaN を「等しい」と返すため
**当該列のソート結果が黙って壊れていた**。投影数がちょうど 2 の冪なら GPU でも
落ちないが、結果は同様に壊れる。つまりクラッシュは氷山の一角で、
**データ破損自体はどのビルドでも起きていた**。

---

## 2. 修正の中核 — 判定の単位を「平均」から「画素」へ

実装は新設の [src/blacklim.h](src/blacklim.h) に集約した (static 実装、ビルドスクリプト変更不要)。

### CT_REC_BLACK_THRESH — 意味の変更

| | 旧 | 新 |
|---|---|---|
| 意味 | ラインの**平均** (I−dark) がこれ未満なら投影を破棄 | **画素ごと**の信号下限。これ未満の画素はこの値で床止めして log を計算 |
| 単位 | dark を超えたカウント数 | 同じ (readme の 1/10/100/1000 のはしごはそのまま有効) |
| 既定値 | 1.0 | **2.0** |
| Inf/NaN 防止 | しない (発火＝破棄のみ) | **する** (床止めにより構造的に発生しない) |
| 0 以下の設定 | 判定が黙って無効化 | **不正値として拒否**し既定に戻す (保護は無効化できない) |
| 有効値の確認手段 | なし (発火時の警告のみ) | **黒投影発生時に stderr へ表示** (2026-08-08 変更。当初は起動時に常時エコーだったが、正常な実行では無出力とした。終了時サマリも床止めが 1 画素以上あった場合のみ) |

床止め後の投影値は `log(I0/しきい値)` で頭打ちになる (既定 2、I0=3000 なら約 7.3)。
床より上の画素は**ビット単位で不変** (10 万点で検証済み)。

もともとこの変数は画素ごとの下限として構想され、検討の過程で平均判定に変わった
経緯がある。実際 `sinog.c` / `of_sinog.c` には原型の画素判定
(`if ((I[jx]-dark[jx]) < 50)`) が残っていた。今回の変更は原点回帰にあたる。

### CT_REC_BLACK_FRAC — 新設

黒投影 (missing angle) の判定を「**床止めされた画素の割合** > FRAC」に置き換えた。

| 項目 | 内容 |
|---|---|
| 既定値 | 0.5 (=ラインの 50%) |
| 有効域 | 0 < 値 ≤ 1 (範囲外は警告して既定維持) |
| 発火時の動作 | 従来どおり: 投影を破棄し、良い投影の平均で置き換える |

板状試料が edge-on になりライン全体が一様に減衰する場合、旧判定 (平均) と
新判定 (割合 0.5) は**同じ透過率で発火する**ことを数値検証で確認した。
つまり missing angle 用途の挙動は保存されている。一方、余白のある高密度試料では
旧判定が原理的に届かなかったのに対し、新判定は実際の損傷量に反応する。

### 実行終了時のレポート

何が起きたかがクラッシュではなくテキストで分かるようになった:

```
CT_REC_BLACK_THRESH = 2 counts above dark (per-pixel floor, max absorbance = log(I0/2))
CT_REC_BLACK_FRAC   = 0.5 of the line
Warning   123  black  clipped=1100/2048 (53.7%, thresh=2)      ← 黒判定された投影
low-transmission guard: clipped 303 / 600 pixel(s) (50.5%),
  minimum signal -3.0 counts, max absorbance 7.313, 3 projection(s) judged black
```

---

## 3. 変更ファイルと各ファイル固有の修正

対象はカウント値から投影値/透過率を作る**実稼働の全 13 ファイル**
(`ct_rec_org.c` はビルド対象外のため未変更)。

| ファイル | 共通変更に加えて直したもの |
|---|---|
| ct_rec.c / ct_rec_c.c / tf_rec.c | 空気基準の **off-by-one** (`po[N*nshot+N-jx]` は jx=0 で次の未書込投影＝未初期化ヒープを読み、最終投影では領域外 1 要素を読んでいた → `N-1-jx` に修正)。**iplc==0 の 0 除算** (全投影が黒判定だと NaN が全面に入る → 明確なエラーで停止)。**calc_center() にも床を導入** (無防備な log が残っており、NaN が入ると RMSD が NaN になり回転中心探索が破綻していた) |
| otf_rec.c / ofct_rec.c | 共通変更のみ (off-by-one 該当箇所はコメントアウト済みだった) |
| sinog.c / of_sinog.c | 原型の画素判定「**1 画素でも** 50 カウント未満なら投影を丸ごと破棄」が残っていた。高密度試料では全投影が破棄され、しかも log(x/0) は素通りしていた → 床止め + 割合判定へ。sinog.c は off-by-one も修正 |
| ct_prj_f.c / ct_prj_f_c.c | 10 カウント以下の画素に**投影値 0.0 (=完全透過!)** を代入していた → 不透明画素として log(I0/床) に |
| ict_prj_fc.c | 該当画素で po[] を**書かずに放置** (前の投影の値が残る) → 床止めに。整数カウントでは発火点は旧 (`>1`) と一致 |
| rhp.c / rhp_c.c / rtf.c (リーダ) | 透過率 `ERROR_VALUE=0.0` は下流で吸収 0=素抜け扱いだった → `床/I_D` に。フレーム判定も割合ベースに統一。rhp_c.c の `ReadHiPicBand` は複数スレッドから呼ばれるため、しきい値キャッシュを `InitReadHiPic` で先に温める |

### NaN/Inf 完全耐性 (再検証で追加)

床のクランプを `if (den < flr)` と書くと **NaN は比較がすべて false で素通り**し、
**+Inf は `< flr` が false で素通り**する。I0 の時刻内挿は `(t2-t1)` で割るため、
隣接 I0 のタイムスタンプが一致すると NaN/Inf が実際に発生する
(rhp.c には "bad time interval" エラーを警告に緩めた跡があり、実データで起きていた)。
クランプを `if (!(den >= flr))` に変更し +Inf は上限で抑え、
`BlackLog()` は**任意のビットパターン入力に対して有限値を返す**ことをテストで保証した。

---

## 4. 数値・挙動が変わる点 (互換性への影響)

1. **off-by-one 修正により再構成値がわずかに変わる。** 旧値は未初期化ヒープ
   (1 層目≈0、2 層目以降は前層の残骸) に依存しており、同じ投影が層の処理順で
   違う結果になり得た。旧結果とのビット一致は失われるが、旧値のほうが不定だった。
2. **sinog / of_sinog は高密度試料で実質別物 (改善)。** 旧: 1 画素でも 50 未満なら
   投影全破棄 → 新: 床止めして保持 (50% 超で破棄)。
3. **リーダ系 (hp_tg / ofct_srec / ofct_DO) の飽和画素**は「吸収 0」から
   「吸収 log(I0/床)」に変わる。
4. **大きなしきい値の副作用が見えるようになった。** 旧設計では大きな値は破棄の
   起こりやすさにしか効かなかったが、新設計では**残る投影の頭打ち値**
   `log(I0/しきい値)` も下げる (床 1000 なら約 1.1)。板状試料以外では既定値を推奨。
5. 既定値が 1→2 になったため、1 カウント台の画素の扱いが変わる
   (旧: そのまま log / 新: 床止め)。dark ノイズを考えればむしろ正しい側。

---

## 5. 検証内容

- **単体**: `blacklim.h` を直接駆動。0/負/NaN/±Inf を含む全入力で有限値
  (旧式は 12 入力中 4 つ + Inf/NaN 系で非有限)。床より上は 10 万点でビット一致。
  一様ライン (板状) では新旧トリガの発火点が一致。環境変数の異常系
  (0 / 負 / 非数値 / 範囲外 FRAC) は実プロセスで拒否とエコーを確認。
- **ビルド**: MSVC 2022 で 13 ファイル全数コンパイル + 実行ファイル 10 本を
  リンクまで完全ビルド。1 バイナリに blacklim.h を含む翻訳単位が 2 つ入らない
  ことを全ビルドレシピで確認 (static 実装の統計が分裂しないため)。
- **統合 A/B**: 芯が不透明な合成円筒データセット (I−dark ∈ [−5..0]) を生成し、
  修正前 HEAD からビルドした旧バイナリと同一データで比較:

  | プログラム | 旧 | 新 |
  |---|---|---|
  | sinog | 3 投影すべて破棄、サイノグラム全ゼロ | 5.5% クランプのみで保持、NaN=0/Inf=0、最大値が log(3000/2)=7.3132 と理論一致 |
  | ct_rec (CPU) | NaN が CBP まで伝播しスライス全ゼロ (GPU なら illegal memory access になる経路) | 有限な再構成 + ガード要約 |
  | ct_rec (全画素不透明の極端データ) | — | iplc==0 ガードが診断を出して停止 |

- 再ビルド後のバイナリは、埋め込まれたガード文字列で「正しいコードが入ったか」を
  照合済み (ct_rec/sinog 系は BlackReport あり、リーダ系はエコーのみ — 各翻訳単位の
  使用関数と一致)。

---

## 6. 運用ガイド

- **通常は何も設定しなくてよい。** 既定 (床 2 / FRAC 0.5) で Inf/NaN は発生せず、
  板状試料の missing angle 処理も従来どおり。
- 実行ログの `clipped=...` と終了時サマリが**透過率不足の定量レポート**になる。
  クランプ率が大きい場合は露光を上げる/エネルギーを下げる判断材料に。
- **視野のほぼ全面を覆う超高密度試料**では、実効クランプ率が「被覆率 × 0.5〜1.0」
  に達し既定 FRAC=0.5 で投影が破棄されることがある →
  `set CT_REC_BLACK_FRAC=0.8` などに上げる。
- 板状試料で従来 `CT_REC_BLACK_THRESH=100` 等を使っていた運用はそのまま通用するが、
  同じ値が**残る投影の頭打ち**にも効くようになった点だけ注意 (§4-4)。
- 全投影が黒判定になると `every projection was judged black ...` で停止する。
  メッセージの指示どおり THRESH を下げるか FRAC を上げる。

関連ドキュメント: [20260723_CT_env_vars.md](20260723_CT_env_vars.md) (環境変数一覧、
2026-08-06 の変更履歴あり) / readme.txt §1e / readme_e.txt §1e
