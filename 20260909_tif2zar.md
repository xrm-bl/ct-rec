# tif2zar — TIFF 連番 → OME-Zarr 変換 (ver 2.5, 2026-09-09)

再構成済み CT ボリューム(連番 TIFF)を、Fiji + BigDataViewer / MoBIE で
快適に閲覧できる OME-Zarr v0.4(Zarr v2)へ変換する。多重解像度ピラミッドと
blosc-zstd 圧縮により、数十 GB のボリュームでも必要なチャンクだけを読んで
軽快にブラウズできる。ソース: `src/tif2zar.c`。

## 1. 使い方

```
tif2zar tifDir out.zarr {--chunk N} {--pixel P} {--min A --max B}
                        {--levels L} {--codec C} {--clevel N}
```

| 引数 | 意味 | 既定 |
|---|---|---|
| tifDir | 連番 TIFF のディレクトリ(uint16 / 32bit float、単一系列) | 必須 |
| out.zarr | 出力ディレクトリ(**既存だとエラー**) | 必須 |
| --chunk | 等方チャンク一辺(偶数) | 128 |
| --pixel | 画素サイズ [µm](タグより優先) | タグ、無ければ 1.0+警告 |
| --min/--max | float32 の量子化レンジ(タグより優先) | 全スライスのタグから合成 |
| --levels | ピラミッド段数 | 最大辺が 256px 以下になるまで自動 |
| --codec | zstd / zlib / lz4 / none(blosc、shuffle ON) | zstd |
| --clevel | 圧縮レベル 1-9 | 3 |

- **出力名は `.zarr` で終えること**。Fiji/MoBIE は拡張子でリーダーを選ぶため、
  付いていないと別形式として誤って開かれる(ver 2.5 以降は警告を表示。
  作成済みのものはディレクトリ名を改名するだけでよい)。
- 連番の**欠番はエラー停止**し、欠けているファイル名をすべて列挙する。
- スレッド数は既定で論理 CPU 数。環境変数 `TIF2ZAR_THREADS` で変更可。
- 実行ログは cmd-hst.log に追記(ct-rec の流儀)。

## 2. 入力メタデータ(ct-rec の ImageDescription 規約)

タグの実体は ImageDescription(TIFF タグ 270)のタブ区切りテキスト:

| 生成元 | 欄 | 内容 |
|---|---|---|
| hp_tg / ct_rec(rec*.tif, float32) | 6 | 画素[µm], 軸位置, 投影数, 角度オフセット, min, max |
| tif_f2i(8/16bit) | 8 | 上記 6 欄 + 規格化 min, max |
| ct_prj_f(p*.tif, float32) | 2 | min, max |

- 画素サイズ = 第 1 欄(µm)。xy の値を z 間隔にも適用(等方前提)。
- float32 のプリパスは全ファイルの**タグだけ**を読み(画素は読まない)、
  全体 min / max を決めて `uint16 = 65535 (v - min)/(max - min)` で量子化。
  NaN は 0 に落として計数、範囲外は半 LSB 以上のはみ出しのみクリップ計数。
- 使用した min/max は出力 `.zattrs` の `ct_rec.physical_min/max` に記録され、
  uint16 から物理値へ逆算できる(uint16 入力でも f2i の 8 欄目までがあれば
  引き継ぐ)。

## 3. 【推奨】rec 直変換では --min/--max を明示する

rec*.tif の desc の min/max は**スライスの実測極値**なので、リングや再構成
ノイズの外れ値がそのままレンジになる。実測(2048³ 実データ)では自動レンジが
-595 .. +493 となり、試料の実質レンジが仮に -0.5 .. 3.0 なら 65536 階調中
約 210 階調しか使われない。**tif_f2i で使う規格化値と同じ --min/--max を
与えるのが良い**:

```
tif2zar rec out.zarr --min -0.5 --max 3.0
```

## 4. 出力構造(OME-Zarr v0.4 / Zarr v2)

```
out.zarr/
  .zgroup  .zattrs      multiscales(全レベルの scale)、omero(表示レンジ=
                        実測値域)、ct_rec(画素サイズ、physical_min/max、
                        元 desc 全文)
  0/ 1/ 2/ ...          解像度レベル(2 倍刻み、2x2x2 の正しい 3D 平均。
    .zarray             奇数端は存在ボクセルのみで平均)
    0/0/z/y/x           チャンク(軸 t,c,z,y,x、t=c=1、<u2、
                        dimension_separator "/")
```

## 5. Fiji での開き方

**パス表記はビューアごとに癖がある**(2026-09、実機で確認)。まとめ:

| 開き方 | 入力するパス | 備考 |
|---|---|---|
| `Plugins → BigDataViewer → N5 Viewer` | **`file:///D:/path/to/out.zarr`**(スラッシュ 3 本) | `file://D:/...`(2 本)は `D:` がホスト名扱いで失敗。素の `C:\...` は旧版 n5 の既知バグで NullPointerException |
| MoBIE `Plugins → MoBIE → Open → Open OME ZARR...` | **素のパス `D:\path\to\out.zarr`**(`file:` を付けない) | `file:` 付きは MoBIE 内部の Paths.get が解釈できず InvalidPathException。**最新の Fiji が必要**(旧版は素のパスが "Cannot get KeyValueAccess" で失敗) |

- MoBIE の導入: `Help → Update... → Manage update sites` で **MoBIE** に
  チェック → Apply changes → Fiji 再起動。古い Fiji では updater が
  "locally modified files" を警告することがあるが、自分で改変していなければ
  Update(上書き)で問題ない。それでも不調なら最新 Fiji を別フォルダに
  展開するのが確実(zip 展開型なので共存できる)。
- ルート(multiscale)を開けばズームに応じてレベルが自動で切り替わる。
  レベル 0/1/2… を個別に開くと各レベルの画素サイズ(2 倍刻み)が付く。
- 表示レンジは omero メタデータから初期化される。手動調整は BDV 上で
  `S` キー、MoBIE ならソースパネルのスライダ。
- 他の閲覧ソフト: napari + napari-ome-zarr(Python、
  `napari D:/path/to/out.zarr`)、Neuroglancer(ブラウザ、要 HTTP 配信)。

## 6. 実測性能(2026-09-09、16 スレッド)

| 項目 | 値 |
|---|---|
| 入力 | rec*.tif 2048 枚、2048²、float32(32 GB) |
| 出力 | 4 レベル、チャンク 128³、4686 ファイル、**8.5 GB**(blosc-zstd 2.16x) |
| 時間 | **1 分 42 秒**(入力スループット ~315 MB/s、ストレージ律速) |
| メモリ | バンド方式(チャンク深さ×スライス)で約 1 GiB + 各レベル縮小バッファ |

## 7. 依存とビルド

- libtiff(既存)+ **c-blosc**(lz4 / snappy / zstd / zlib 込み)。
  vcpkg の `x64-windows-static`(/MT)でビルドした .lib を `src/` に同梱
  (`libblosc.lib`, `lz4.lib`, `snappy.lib`, `zstd.lib`。zlib は既存の
  `zs.lib`)。入手手順は 20260717_LIBTIFF-MIGRATION.md と同じ流儀
  (vcpkg.json に blosc を追加済み)。
- JSON は手書き出力(構造が固定のため)。`setlocale(LC_NUMERIC,"C")` で
  小数点記号を固定。
- Linux は `-lblosc -lzstd -llz4 -lsnappy -lz`(共有システムでは静的リンク
  推奨、MakefileCPU のコメント参照)。

## 8. 検証

- 独立実装の参照コード(素朴なダウンサンプラ+ c-blosc 展開)と
  **全ボクセルのバイト一致**を確認: uint16 / float32、奇数寸法の全レベル、
  端チャンクのゼロ詰め、NaN・クリップ処理、zstd / zlib / none の各コーデック。
- 欠番検出(ファイル名列挙して exit 1)、--min/--max 上書き、自動レベル数。
- Fiji(BigDataViewer / N5 Viewer)で実表示確認: 断面ブラウズ、レベル切替の
  連続性、タグ由来の画素サイズがスケール表示と一致、blosc 圧縮データの展開。
