# ct_prj_f への Paganin 位相回復の導入 (ver 2.5, 2026-09-09)

投影像生成プログラム `ct_prj_f`(ソース: `src/ct_prj_f_cp.c` + `src/paganin.h`)
に、単一距離の位相回復(Paganin 法)をオプションとして追加した。旧来の
mkpms / mkpmbg / pm_sino 連鎖(SPring-8 で長年使われてきた別系統のツール群)を
置き換えるもので、両者が数値的に等価であることを検証済みである。

## 1. 使い方

```
ct_prj_f HiPic prj            ← 従来どおり(吸収のみ)。結果も従来と同一
ct_prj_f HiPic prj pr.par    ← Paganin 位相回復を投影ごとに適用
```

pr.par は 5 行(1 行 1 数値、`#` または `//` 以降はコメント可):

| 行 | 意味 | 単位 | 制約 |
|---|---|---|---|
| MU | 線吸収係数 μ | cm⁻¹ | > 0 |
| DELTA | 屈折率デクリメント δ | - | > 0 |
| R1 | 光源-試料距離 | cm | > 0(平行ビームでは未使用) |
| R2 | 試料-検出器距離 | cm | ≥ 0 |
| P_SIZE | 実効画素サイズ | **µm** | > 0 |

出力は吸収時と同じ `prj/p%05d.tif`(32bit float)で、値は μt = -ln(T′)。
そのまま `p_rec_G_F` などで再構成すれば μ の断面が得られる。低透過率ガード
(`CT_REC_BLACK_THRESH`)は位相回復後の透過率にも同じ床で効く。

## 2. 【重要】P_SIZE の単位変更(cm → µm)

旧 mkpms / mkpmbg 系の pr.par は P_SIZE を **cm** で取っていた。本実装は
**µm** である。旧ファイルを流用すると 4 桁の取り違えとなり、フィルタの
k² 項が 10⁸ 倍ずれて、ほぼ無フィルタ(逆方向なら過剰平滑)の結果が黙って
出る。実際にこの事故が起きたことが導入の教訓になっている。

防御は 2 段階入っているが、完全ではない:

- P_SIZE < 1 nm はエラー停止(旧 cm 単位ファイルの典型値を検出)。
- パディング後サイズが上限(32768)を超えた場合のエラーに単位のヒントを表示。

**すり抜けるケースがある**ため、起動時に表示される
`rc=…px, pad=…`(平滑化半径と余白幅、画素単位)が物理的に妥当
(通常 rc は数画素)であることを必ず確認すること。

## 3. アルゴリズムと実装

Paganin et al., *J. Microsc.* **206**, 33-40 (2002) の式 (10)、平行ビーム
(M = 1)。透過率 T = (I-dark)/I0(I0 は前後の I0 フレームから時間線形補間)
に対し、k 空間でフィルタ 1/(1 + (R2 δ/μ)|k|²) を掛けて T′ を得る。

- **境界処理**: ミラー(対称)パディング。幅は核の実空間減衰長
  rc = sqrt(R2 δ/μ) の 10 倍(最低 32 画素)。ゼロ埋めと違い平坦場の応答が
  厳密に 1 になるため、旧 mkpmbg のような背景マップの減算が不要。
- **FFT**: 自前の radix-2 複素 FFT(外部ライブラリ不要)。単精度・実数
  パック(実数 2 行を複素 1 本に詰めて変換)・エルミート対称による列の
  半減・OpenMP 並列。パディング後サイズは 2 のべき乗に切り上げ。
- **メモリ**: パディング後 1 画素あたり約 12 バイト
  (2048² フレーム → 4096² で約 100 MB)。
- **スレッド数**: 環境変数 `PAGANIN_THREADS`(既定 = 論理 CPU 数)。
  リング除去用の `OMP_NUM_THREADS` とは独立。

## 4. 検証

すべて合成データによる機械検証(検証コードはリポジトリ外)。

| 検証項目 | 結果 |
|---|---|
| 単精度 FFT vs 素朴な倍精度 DFT(n=64) | max 1.3e-6 |
| 順→逆変換の往復 | max 2.2e-7 |
| カーネル≡1(R2=0)での恒等性 | max 2.9e-7 |
| フィルタ全体 vs 倍精度総当たり参照(128²) | max 1.8e-7 |
| 倍精度実装との比較(2048²、ファントム+1%ノイズ)の p = -ln T′ | max 1.4e-6 / rms 1.3e-7 |
| **旧 mkpms+mkpmbg 連鎖(背景減算後)との一致**(256×128) | **max 4.3e-7**(32bit float 量子化と同水準) |
| 実行ファイル一気通貫(合成 HiPic、吸収/位相の両モード) | 吸収は解析値と一致、位相は期待どおりの平滑化 |

いずれの誤差も透過データのフォトンノイズ(I0 = 10⁴ カウントで p に ~1e-2)
より 4 桁以上小さい。

## 5. 性能

| 条件 | 時間 |
|---|---|
| 2048² 投影 1 枚、16 スレッド | 約 0.10 秒 |
| 同、1800 投影のスキャン | 約 3 分 |
| 参考: 初版(倍精度・複素 FFT) | 0.65 秒/枚(6.8 倍遅い) |

## 6. 旧 mkpms / pm_sino 連鎖との既知の相違

数値等価なのはフィルタ本体である。連鎖全体では次の違いが残る。

1. **pm_sino のライン傾斜補正は行わない**。旧 pm_sino は各行の両端が 0 に
   なるよう一次式を引いていた(空気領域前提のベースライン補正)。本実装は
   引かない。必要ならシノグラム段階で別途行うこと。
2. **境界処理**: 旧はゼロ埋め(横方向は Nx が 2 のべき乗ちょうどのとき巡回)
   + 背景マップ減算、新はミラーパディング。試料が視野端にかかるデータでは
   端から ~10·rc の範囲で挙動が異なる(新の方が素直)。
3. **T′ ≤ 0 の画素**: 旧は -ln|T′|(絶対値)、新は低透過率ガードの床で
   log(I0/しきい値) に頭打ち。
4. 旧 .pm(倍精度、背景込み)と新 p*.tif(32bit float、背景不要)を
   **直接比較してはいけない**。比較するなら旧側は
   (mkpms の .pm) − (mkpmbg の .pm) を作ること。

## 7. 参考文献

- D. Paganin, S. C. Mayo, T. E. Gureyev, P. R. Miller, S. W. Wilkins,
  "Simultaneous phase and amplitude extraction from a single defocused
  image of a homogeneous object", *J. Microsc.* **206**, 33-40 (2002),
  doi:10.1046/j.1365-2818.2002.01010.x
